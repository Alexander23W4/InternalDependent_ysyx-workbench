step by step deep analysis AM Makefile

guest Makefile: 
NAME = demo
SRCS = $(shell find src -name "*.c")
include $(AM_HOME)/Makefile

Gets  Items  Flags  Rules(item build command (ic)/ operation command (oc)) 

-> Get: SRCS  NAME  ARCH(ISA PLATFORM)

-> Items:  WORK_DIR    DST_DIR

            IMAGE(target bin)  
            ARCHIVE(target lib)   OBJS(intermediate .o targets, [SRCS].o)   LIBS(amlibs, includes: am, klib)  LINKAGE(all things should be linked, OBJS(.o))  

-> Flags: INC_PATH(guest .h) 
            [ INC_PATH += $(WORK_DIR)/include $(addsuffix /include/, $(addprefix $(AM_HOME)/, $(LIBS))) ]
            [ INCFLAGS += $(addprefix -I, $(INC_PATH)) ]

-> External Inc: scripts/ARCH.mk





MAKECMDGOALS: run  archive  gdb




