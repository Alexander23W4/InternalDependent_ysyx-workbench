结构化 basic component:
MUX  Decoder/Encoder  ALU  Counter Memory  Shift/Barrel Shifter   Finite State Machine    KeyBoard_input     VGA

Verilog was not originally intended for designing synthesizable circuits; its essence is a circuit modeling language based on an event queue model. 
注意上面这句话， 所以behavior modelling大部分情况下只适合原型验证， 如果要design cutting edge processer, 必须上structure

CPI (clk per instruction)

behavior modelling   things need to concern:
使用的场景： 原型验证  IP核内部   参数化高可复用场景
使用structure modelling的场景： 
1. **性能极限**：超高频需要手工时序优化
2. **面积极限**：门级手工优化省5%面积
3. **特定工艺**：硬核（版图级）流片用

结构描述（YSYX高手）：
时钟：0ns    5ns    10ns   15ns   20ns   25ns
     ┌──────┼──────┼──────┼──────┼──────┼
IF   │ pc=0 │ pc=4 │ pc=8 │ pc=C │ ...  │
     │inst0 │inst1 │inst2 │inst3 │     │
     ├──────┼──────┼──────┼──────┼──────┼
ID   │      │inst0 │inst1 │inst2 │inst3 │
     │ rs10 │ rs11 │ rs12 │ rs13 │     │
     ├──────┼──────┼──────┼──────┼──────┼
EX   │      │      │inst0 │inst1 │inst2 │
     │      │      │add=8 │add=9│     │
     ├──────┼──────┼──────┼──────┼──────┼
MEM  │      │      │      │add=8 │add=9│
WB   │      │      │      │      │add=8 │

每个格子=15ns，流水线并行 → 250MHz
用5级流水线手工打断所有长路径，时序收敛。  inst pipeline: 

1. **约束降低频率**：200MHz→180MHz（偷懒）
2. **综合优化**：retiming寄存器插入（自动70%）
3. **结构流水线**：手工打断关键路径（YSYX核心）
4. **Floorplan**：关键模块固定区域
5. **物理优化**：手工移动违例cell

结构描述强制每15ns保存中间结果，综合器只能顺着走！

3. **结构流水线（手工打断）**
   ├─ 5级流水线IF-ID-EX-MEM-WB  ← YSYX基础
   ├─ **指令字段并行解码**      ← 你书上看到的！
   ├─ 寄存器文件双端口读         ← 并行rs1+rs2
   └─ 多ALU并行执行

流水线冲刷机制：
实际运行时：
时钟0ns：IF取inst0=beq x1,label  (pc=pc0)
时钟5ns：IF取inst1=???           (pc=pc4)  ← 已经开始了！
时钟10ns：ID发现是beq跳转！

这时两种情况：
- 不跳转：inst1是对的，继续流水
- 跳转：冲刷错误指令，重取label地址

真实商用CPU例子：ARM Cortex-A78
text
- 14级前端流水（IF）
- 6级执行流水（ALU/分支）
- 4级缓存流水（MEM）
- 3级写回流水（WB）
- **总27级**，1.8GHz (28nm)

结构化描述时序边界清晰， 对于path掌控感强
结构化描述方便 synthesis的时候调试，调试结果attribute直观， 如果path太长， 直接拆级


