From RTL code to Netlist:

Synthesizer & PDK & EDA  (PDK$: Design Rules, Device Model, Process Constraints)

PDK backup Synthesis $
*Do: -> Parsing  .v => AST (lexical+sytax)
     -> Elaboration instantiation specification   AST => RTLIL(RTL intermediate language) 
     -> Coarse-Grain Synthesis

*yosys operation: 
    yosys *.v
    hierarchy -check -top counter
    dump
    write_rtlil
    show (xdot, rtlil virtualization)
    proc (optimization)
    opt  (optimization)
    fsm
    memory
    techmap (fine-grain) (the first step is to map word-level cells to gate-level cells)
    splitnets -ports (split some multi-bit nets and ports. Otherwise, RTLIL will contain unnecessary bit extraction and bit concatenation operations. )
    (technology mapping)
    read_liberty -lib (xxx).lib
    dfflibmap -liberty (xxx).lib
    abc -liberty (xxx).lib
    (the second step is to map gate-level cells to standard cells of the target technology.)
    write_verilog netlist.v
    stat -liberty (xxx).lib
    




fan out => circuit delay     cell amount   merge cell   duplicate cell
^($*)opt: choosing between merging cells and duplicating cells is actually a trade-off between area and performance.

In addition, different memory specifications have different shapes, which will also affect the subsequent placement and routing. These mutually restrictive factors make it difficult for synthesizers to automatically identify and map memories. Therefore, RTL developers need to select memory specifications according to design goals and manually instantiate memory units as submodules in the RTL code.























