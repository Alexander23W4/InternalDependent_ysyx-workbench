整个系统涉及3个设备: timer, vga, keyboard
涉及 TRM 和 IOE  

-> 游戏的总体逻辑

读取用户输入  更新游戏逻辑   输出新的画面    (这三个要同步进行)
流程控制: frame(预期桢数)  current(逻辑桢数)  rendered(画面桢数), 目的是让画面桢数 和 逻辑桢数都跟上 预期桢数

-> 游戏的进程

--> 游戏的启动:    加载IOE-API, 初始化屏幕, 初始化游戏素材

--> 游戏的单周期进程: 
    游戏的操作对象: 字符character, 状态: 闪退, 运动(触顶, 触底, 运动)     运动->击中->触顶   运动->触底->闪退

    更新游戏逻辑(game_logic_update): 生成新字符, 更新已有字符(状态, 位置)
    读取用户输入(check_hit): 更新字符状态, 更新积分
    输出新的画面(render)


用户成功按下一个字母并实现命中时，整个计算机系统（包括Verilator 仿真环境、NPC RTL 处理器、AM 抽象层、输入输出设备、运行时环境及打字游戏程序本身）是如何协同工作的:

NPC-RTL(N)  AM  IO(IO设备及其总线接口)  G(打字游戏)  V(Verilator仿真环境)

1.用户成功按下一个字母A   (IOD)
2.device_update() 读取SDL event并且捕获 enqueue(A的am_scancode)  (IO)
3.游戏周期调用io_read(AM_INPUT_KEYBRD);  (G)
4.io_read(AM_INPUT_KEYBRD);展开, 调用AM_IOE API __am_input_keybrd(AM_INPUT_KEYBRD_T *kbd) 调用 *(volatile uint32_t *)NPC_KBD_ADDR;(AM)
5.编译成 lw xx NPC_KBD_ADDR, NPC RTL经过Fetch Decode 识别出lw, 计算出addr和, 通过DPI-C接口调用ram_read()函数 (N)
6.Verilator-C 执行ram_read(), 发现不在mem范围内从而MMIO重定向到map_read(),检索定向发现对应keybaord, 执行i8042_data_io_handler() (V)
7.i8042_data_io_handler执行总线逻辑, dequeue取出字母A的码  (IO)
8.ram_read()将值返回给RTL, RTL将返回值作为wdata, 拉高wen, 计算出raddr, GPR在下一个上升沿更新目标寄存器的值  (N)
9.游戏读出keycode并且转化为字符'A'      (G)
10.check_hit扫描字符表找到最下方的'A'并且改变其运动方向和速度, hit++  (G)
11.游戏周期调用render(),render多次调用API接口io_write(AM_GPU_FBDRAW),先调用API擦除上一桢的所有写入, 再遍历字符表char调用API写画布, 最后再调用API刷新screen (G)
12.io_write(AM_GPU_FBDRAW)调用__am_gpu_fbdraw(), 内部*(volatile uint32_t *)(NPC_FB_ADDR + vmem_index)写入画布地址, *(volatile uint32_t *)SYNC_ADDR = 1刷新 (AM)
13.NPC MMIO将FB_ADDR定向到 画布对应物理地址, 将VGA_CTL_MMIO定向到 vga_update_screen()  (N, V)
14.画布物理地址写入操作, vga_update_screen()实现交互逻辑, 如果SYNC = 1则硬件刷新screen. 更新后显示新的画面给用户, 实现游戏输入输出交互周期  (IO)
15.之后FPS个周期, 多次执行9-12步骤, 直到画面中'A'字母碰到屏幕上沿立即消失, 用时1s  (G)



2. 运行观察方法
  描述您是如何观察、调试和理解打字游戏在 NPC RTL 处理器上的运行机制的。包括您使用的工具（如 Verilator 的波形查看器、NPC 的调试输出、日志功能、信号追踪等）、
  调试的方法和观察的角度（如 RTL 级信号变化、处理器周期、指令执行过程等），
  以及这些观察是如何帮助您理解程序运行的。


C语言的printf输出
ftrace(NPC之上的函数调用)
调试的方法gdb, sdb 定位具体instr, 具体pc (NPC之下的Verilator-C以及处理器周期)


3. 印象深刻的部分
  在您理解打字游戏运行的过程中，介绍哪一部分您最为印象深刻，并解释为什么。例如：Verilator 仿真机制、RTL 仿真调试过程等？

-> 游戏周期的完整交互流程是怎么实现的,  game_logic_update  check_hit  render, 在交互周期内是如何被调用从而实现同步的

-> DPI-C函数ram_read()多次调用, 导致dequeue被覆盖引发的bug





