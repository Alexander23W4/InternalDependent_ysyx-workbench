
Large program exp:
-> meta programming framework 
-> Config(customer setting) generate config file (Macro...or other info for building the functional could purposefully)
    (scripts take the customer setting & automatically generate macro is normally used) 
-> src & Debug env 
    (debug env is critical, especially debug only a tiny area among the large systematic code, usually encounter overlap/ meta-rely problem)
    (gen testbench, automatic testing, code with debug macro & debug info, partial independent testing)
  Debug Pricinples:
  ^^Turn as many fault as possible into error
  ^^Catch the moment as soon as the potential fault occurs
  ^^Tools: -Wall GDB  printf(log) assert() sanitizer

The memory is a large array defined in nemu/src/memory/paddr.c.
PCs and general-purpose registers are defined in structures in nemu/src/isa/$ISA/include/isa-def.h
The adder is defined in... Well, this part of the framework code is a bit complicated, but it doesn't affect our understanding of TRM, so let's go over it in PA2!
The way TRM works is reflected in cpu_exec() and exec_once().

-> 保持系统的固有特性，保证每个component简单性，易于维护，易于迭代，完成它固有的使命。只需保证一般兼容性和一般完整性
   在建立系统的初期，应该先建立框图，快速打通系统的大致功能，框图的每一部分完成最简单最基础的功能，无需考虑完整性和兼容性
   after that,如果系统初步验证大致符合预期，则通过不断迭代优化丰富完整性和兼容性

   学习复杂的东西也是一样，先确立框架，建立与熟悉最基本的开发流程和系统框架，breakdown to very simple, 之后深入理解这个东西的痛点，工程上的趋势，怎么
   样让他真的变得更好，有自己的idea与艺术，并不断迭代实践。
   系统的每一个node都做的尽量简单，但是在运行之前必须debug,而且要顺着hiereachy自下而上一点一点debug和test,系统中没有经过debug和单独test过的部分永远都是错的
   所有系统都是hiereachy,系统内部还有系统，系统的层次结构应当简单明了

   为人也保持自身的独立性和自我意志，不追求过度的与大量其他个体与环境的兼容性和所谓某些哲学性上人的完美
     
$$$ [RT-Thread]
The key to the batch processing system is to have a background program that automatically 
  loads a new foreground program for execution when one foreground program finishes execution.

-> The function of first OS:
After the user program finishes execution, it can jump to the operating system's code to continue execution
The operating system can load a new user program to execute

-> OS as administrator:
Generally speaking, the most suitable role for a system administrator is the operating system itself, 
  which has the highest privilege level and can perform all operations. 
  Unless specifically allowed, user programs running on the operating system generally have the lowest privilege level. 
  If they attempt to disrupt social harmony, they will be sentenced to "death".

-> priviledge modes:
Taking RISC-V processors that support modern operating systems as an example, they have three privilege modes: 
  M, S, and U, representing machine mode, supervisor mode, and user mode, respectively.

-> accomplish privildge system:
It only needs to maintain a register in hardware to identify the current privilege mode (which is part of the computer's state), 
  and then check the current privilege mode when accessing resources that can only be accessed by higher privilege levels.

For example, RISC-V has a privileged instruction sfence.vma, 
  and the manual requires that it can only be executed when the processor's current privilege mode is not lower than S mode. 
Therefore, we can add some simple logic in hardware to implement the privilege mode check:

is_sfence_vma_ok = (priv_mode == M_MODE) || (priv_mode == S_MODE);


$$$ [Trace] ;
RTFSC + flag + defensive programming(assert, printf) + gdb + trace

We already know that a program is a state machine, and if the program becomes complex, so do the state machine's transfers. 
Not to mention the details of each state in the state machine, it's hard to fully understand how the state machine transfers between states.

It would be inefficient to use GDB to understand this process. 
To improve efficiency, we can use printf() to output some of the information we care about. 
The fact that we care about the details of how this state-machine transfer in its states it also means 
  that we care about the execution of the program. In the field of software engineering, 
  the information that records the execution of a program is called trace. 
  With the trace information, we can determine whether the program execution process is as expected, and thus diagnose bugs

instr_fetch(), disassembly(pc, binary-instr, assembly)
The implementation of itrace is very simple, the code just records each instruction fetched by instr_fetch(), 
  and then calls the disassembly function provided by the llvm project (implemented in nemu/src/utils/disasm.cc). 
  Itrace outputs the PC, binary representation of the instruction and the disassembly result. 

->itrace: trace instr
->ftrace: trace functions
   We can record the destination address in the function call instruction, indicating that a function will be called. 
   Then we can record the current PC in the function return instruction, 
   indicating that we are returning from the function where the PC is located.
   
riscv64-linux-gnu-readelf -a add-riscv32-nemu.elf


%%% [Game]:
These games may look very different, but they all embody the framework of "how programs use IOE to achieve game effects". 
In fact, a game can be abstracted as an infinite loop:

while (1) {
  wait_for_new_frame();  // AM_TIMER_UPTIME
  process_user_input();  // AM_INPUT_KEYBRD
  update_game_logic();  // TRM
  draw_new_screen();  // AM_GPU_FBDRAW
}

$$$ [Device]:
I/O addressing method   Device addresses

That's exactly it! Devices also have their own status registers (equivalent to CPU registers) 
and their own functional units (equivalent to CPU arithmetic units).

Of course, different devices have different functional units, 
for example, the keyboard has a component that converts the analog signal of the key press into a scan code, 
while the VGA has a component that converts the pixel color information into an analog signal for the display.

The signal that controls the operation of the device is called a "command word", 
which can be understood as a "device instruction". 
The job of the device is to receive the command word and perform decoding and execution... 

Accessing devices = reading data + writing data + controlling status.

since devices also have registers, 
a simple way is to use the device's registers as the interface and let the CPU access these registers.

We can also assign numbers to the registers in the device that the CPU is allowed to access, 
  and then reference these numbers through instructions. 
There may be some private registers in the device that are maintained by the device itself, 
  they do not have such numbers, and the CPU cannot directly access them.

-> I/O stuations
When executing ordinary instructions, the state machine transitions according to the TRM model

When executing device output-related instructions (such as the out instruction in x86 or the MMIO write instruction in RISC architectures), 
  the state machine remains unchanged except for updating the PC, but the state of the device and the physical world will change accordingly

When executing device input-related instructions (such as the in instruction in x86 or the MMIO read instruction in RISC architectures), 
  the state machine transition will "branch": the state machine no longer has a unique new state like in the TRM, 
  which new state the state machine will transition to will depend on the state of the device when executing this instruction


-> DMA utilization:
We know that S = <R, M>, and the port I/O and memory-mapped I/O introduced above all perform data interaction through the register R. 
  Naturally, we can consider whether there is an input/output method that performs data interaction through memory M.

Actually, there is such a method called DMA. To improve performance, most complex devices generally have DMA functionality. 
  However, the devices in NEMU are relatively simple, so we will not go into the details of DMA.

-> support Devices (hardware architecture level)
NEMU implements seven devices: serial port, clock, keyboard, VGA, sound card, disk, and SD card, 
  among which the disk will be introduced at the end of PA, and the SD card will not be involved in PA. 

-> support AM API (runtime env level):
IOE (IO extension API) provides three APIs:

bool ioe_init();
void ioe_read(int reg, void *buf);
void ioe_write(int reg, void *buf);


Note that the reg register here is not the device register discussed earlier, 
  because the numbering of device registers is architecture-dependent. 
  In IOE, we want to adopt an architecture-independent "abstract register". 
  This reg is actually a function number, 
  and we agree that the meaning of the same function number is also the same across different architectures, 
 thus achieving the abstraction of device registers.

// ----------------------------------------------------------------------------------------------------------------------

SYSTEMIC UNDERSTANDING:
🔹 Runtime Environment（运行时环境）
指：
程序在“运行时”所依赖的整体环境
它包括：
操作系统
进程模型
内存布局
栈结构
加载器（loader）
动态链接器
libc
运行时库
系统调用接口
以及 ABI 规则

🔹 ABI（Application Binary Interface）
ABI 是：
二进制层面的接口规范
它规定：
函数调用怎么传参
返回值放哪里
寄存器如何使用
栈如何增长
数据类型大小
对齐规则
调用约定（calling convention）
可执行文件格式（例如 ELF）


例如在 RISC-V Linux 上：

ABI 会规定：
参数放在：
a0–a7 寄存器

返回值：
a0
栈向下增长
结构体对齐方式
ELF 格式

而 runtime environment 还包括：
Linux 内核
动态链接器 ld.so
glibc
启动代码 _start
程序入口流程


ABI 有两种层次
1. 用户态 ABI (compiler decided)     CPU-core related, Compiler responses
解决
函数调用规则
数据布局
寄存器分配

2. 系统调用 ABI (printf interrupt... )  customer code (user mode) could not decided       CPU-control & SoC-system related, OS responses
解决
用户如何请求内核服务
内核如何接收参数
内核如何返回结果
这部分：是 OS 必须定义的



OS CRITICAL COMPONENT:

CPU 上电后会做一件极其简单但关键的事：
PC（程序计数器） = 固定地址
这个地址在不同架构是硬编码在 CPU 设计里的。
this addr is projected to BIOS/UEFI (in flash mem) 

->BIOS/UEFI 
BIOS 做的事情：
初始化内存（RAM）
初始化设备
找启动设备（硬盘）
加载 bootloader

->Bootloader
; 从磁盘读 kernel 到内存
; 跳转到 kernel entry
jmp kernel_main

->CPU 模式切换
real mode → protected mode → long mode（x86）
必须做：
开启分页（paging）
设置段寄存器
初始化栈

->内存管理
物理内存管理
虚拟内存管理
内核堆

->Process & task 进程&任务系统
上下文切换  调度器

->中断系统（Interrupt / Trap）
核心作用：
CPU 和外界交互

你需要：
（1）IDT（中断描述符表）
interrupt number → handler
（2）中断处理函数
void timer_handler();
void keyboard_handler();
（3）系统调用（Syscall）
用户态 → 内核态：
int 0x80
或现代：
syscall 指令

->驱动（Drivers）

最基础必须有：
（1）定时器（Timer）
用于调度
（2）串口 / 控制台
输出日志
（3）键盘（可选）


->File system 文件系统

->User Space 用户管理

->Command line

->API


OS 's purpose:

more abstraction, convient for multi- modern large program run at the same time
CPU → 多进程
内存 → 独立空间
thread   visual-memory   files  owners  jobs(process)...

and allocation (monitor, management)
谁用 CPU？
谁用内存？

-------------------------------------------------------------------------------
2026.4.4:
对于一台 PC 来说，运行时环境（Runtime Environment）并不仅仅是操作系统（OS），而是“Boot 固件(not exclusively) + 操作系统(only kernel) + 运行时库(lib)”的层层组合。

为了帮你分清边界，我们可以从 PC 开机到程序运行的完整过程来看：

1. Boot 固件（BIOS/UEFI）—— 硬件与 OS 之间的“胶水”
当你按下电源键，CPU 一开始根本无法执行操作系统（因为硬盘里的 OS 还没有被加载到内存）。最先运行的是主板上的 Boot 固件。

它提供最底层的运行时环境：

CPU 初始化（设置缓存、内存控制器）

枚举硬件（PCIe 设备、USB、硬盘、显卡）

提供基本的输入输出服务（在 UEFI 环境下，甚至能运行图形界面和网络驱动）

最终找到启动设备，加载 OS Loader 到内存，并把控制权交给 OS。

它算不算 Runtime Env？ 算。 对于任何运行在 PC 上的软件（包括 OS 本身），Boot 固件是第一个“环境”。即使 OS 启动后接管了所有硬件，Boot 固件的一些服务（如 ACPI 表、UEFI Runtime Services）在 OS 运行时仍会被调用（例如关机、休眠、修改启动顺序）。

2. 操作系统（OS）—— 主要的运行时环境
OS 启动后，提供程序员和用户最常打交道的环境：

进程调度

内存管理

文件系统

设备驱动（接管并超越 Boot 固件的简单驱动）

系统调用接口

对于普通应用程序（比如 Chrome、Word），它们的直接运行时环境就是 OS Kernel + 动态链接器 + C 标准库（libc）。



























































